# communication.py
# this file currently has no implementation; define
# a stub so imports from other modules succeed.

def run_communication_mode(hand_landmarks, frame=None):
    """Placeholder for communication mode.

    The real logic isn't implemented yet, but providing a
    no-op function prevents import errors and lets the
    GUI start with the other modes working.

    ``frame`` is optional; some modes accept it for
    drawing overlays (e.g. entertainment).
    """
    # TODO: implement gesture-driven communication features
    # until then, just ignore the input
    pass
